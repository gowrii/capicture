<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div data-magellan-expedition="fixed">
      <dl class="sub-nav">
        <dd><strong>CAPICTURE</strong></dd>
        <dd data-magellan-arrival="games"><a href="/games/">Games</a></dd>
        <dd data-magellan-arrival="boards">Boards<a href="#"></a></dd>
        <dd data-magellan-arrival="users">Users<a href="#"></a></dd>
      </dl>
    </div>

    <div class="row">

      <div class="large-8 columns">
        <%= yield %>
      </div>

      <div class="large-4 columns">

        <div class="row">

          <div class="panel login-panel">
            <% if current_user %>
              <div data-alert class="alert-box">
                Logged in as <%= current_user.email %>.
                <a href="#" class="close">&times;</a>
              </div>
              <%= link_to "Log Out", logout_path, class: "button radius" %>
            <% else %>
              <%= link_to "Log In", login_path, class: "button radius" %>
              <%= link_to "Sign Up", signup_path, class: "button radius" %>
            <% end %>
          </div>

        </div>

      </div>

    </div>
    
    <%= javascript_include_tag "application" %>
  </body>
</html>