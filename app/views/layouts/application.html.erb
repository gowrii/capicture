<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "CAPICTURE" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div data-magellan-expedition="fixed">
      <dl class="sub-nav left">
        <dd><strong>CAPICTURE</strong></dd>
        <dd data-magellan-arrival="games"><a href="/games/">Games</a></dd>
        <dd data-magellan-arrival="boards"><a href="#">Boards</a></dd>
        <dd data-magellan-arrival="users"><a href="#">Users</a></dd>
      </dl>
      <dl class="sub-nav right">
        <% if current_user %>
          <dd data-magellan-arrival="games">Logged in as <%= current_user.email %>.</dd>
          <dd data-magellan-arrival="logout"><a href="/logout/">Log Out</a></dd>
        <% else %>
          <dd data-magellan-arrival="login"><a href="/login/">Log In</a></dd>
          <dd data-magellan-arrival="signup"><a href="/signup/">Sign Up</a></dd>
        <% end %>
      </dl>
    </div>

    <div class="row">
      <div class="large-8 columns">
        <%= yield %>
      </div>
    </div>
    
    <%= javascript_include_tag "application" %>
  </body>
</html>